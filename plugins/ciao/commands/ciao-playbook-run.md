---
name: ciao-playbook-run
description: Trigger a Ciao playbook run. Args: playbook id or name, optional project.
---

Use the `ciao-playbook-run` skill.

If the user passed a name (not an id), list playbooks first to resolve
the id. If the playbook has runtime parameters, tell the user the form
rendering only works in the browser and link them to the QA dashboard.
