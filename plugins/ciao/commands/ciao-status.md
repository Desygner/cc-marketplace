---
name: ciao-status
description: Show a Ciao project's state — blocks, env var names, branch. Arg: project name or slug.
---

Use the `ciao-status` skill to fetch a Ciao project's metadata, installed
blocks, and env var names.

If the user did not pass a project name, list projects first and ask
which one to inspect.
